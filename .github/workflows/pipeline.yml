name: Main Mock
on:
  push:
    branches:
    - "main"

env:
  VERSION: v5

jobs:
  step-1:
    uses: ./.github/workflows/step-1.yml
    name: Test
  step-2:
    uses: ./.github/workflows/step-1.yml
    if: 1 == 2
    name: Test 2
  step-3:
    runs-on: ubuntu-latest
    needs: [step-1, step-2]    
    environment: dev
    steps:
      - name: broa
        run: |
          echo 'Henrique'
          echo '${{ vars.jacutinga }}'
          mkdir '${{ vars.jacutinga }}'
